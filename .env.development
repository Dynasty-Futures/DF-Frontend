# =============================================================================
# Dynasty Futures Frontend - Development Environment
# =============================================================================
# Loaded when running: npm run dev
# The Vite dev proxy forwards /v1/* requests to the local backend,
# so VITE_API_URL is left empty (requests stay same-origin).
# =============================================================================

# Backend API URL (empty = use Vite dev proxy to localhost:3000)
VITE_API_URL=

# Use mock data instead of real API (set to true to bypass backend)
VITE_USE_MOCK_DATA=false

# Enable debug logging in the browser console
VITE_DEBUG_MODE=true
